export type productWithoutClassify = {
    id_category: number,
    description: string,
    id_brand: number,
    images: string[],
    slug: string,
    info_detail: string[],
    name: string,
    price: number,
    quantity: number
}